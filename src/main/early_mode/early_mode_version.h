/*
 * Rodzhang
 *
 */

#ifndef __EARLY_MODE_VERSION_H__
#define __EARLY_MODE_VERSION_H__

#include <stdio.h>

const char *early_get_version(char *buf);

void early_mode_version(void);

const char *spate_version_get(void);




#endif /* __EARLY_MODE_VERSION_H__ */
